/// <reference types= "cypress" />

describe('Deletar empresa', ()=>{
    // let id
    // before(()=>{
    //     // criar empresa
    //     // pegar id
    // })

    it('Deve ser possivel deletar empresa atraves do ID', ()=>{
        // cy.request({
        //     method: "PATCH", 
        //     url: "http://localhost:8400/api/company/" + id + "/delete", 
        // })

        // // verificando se continua sendo listada
        // cy.request({
        //     method: "GET", 
        //     url: "http://localhost:8400/api/company/" + id
        // }).then((response)=>{
        //     expect(response.body).to.be.empty()
        // })
    })

    // o teste validando que é possivel deletar foi realizado manualmente para não conflitar com os outros já que a criação não funciona
})